Task 1: complete (commits 75fb2f2..8fc70dc, review clean)
Task 2: complete (commits 8fc70dc..9f91e9f, review clean)
Task 3: complete (commits 9f91e9f..f75bb93, review clean)
Task 4: complete (commits f75bb93..5a0b603, 1 fix cycle: RequestException handling, review clean; 4 Minor findings carried to final ledger: dead ImportError fallback in fetch_cepa_movilizacion, no pacing on _descubrir_actas POST, display:none regex not case-insensitive, helper placement far from sibling HCDN helpers)
Task 5: complete (commits 5a0b603..bc9751b, 1 fix cycle: empty-bloque regression test, review clean; found real 6-column table structure diverging from 3-column assumption, corrected; Minor carried: 257-row Wayback validation not committed as fixture)
Task 6: complete (commit bc9751b..11a89a6, review clean, no fix needed); Minor carried: unidad string hardcodea '90 días' sin usar dias_ventana real, backfill necesita dias_ventana!=90 (ya cubierto por diseño de Tarea 10), sin test del branch r is None por-acta
Task 7: complete (commits 11a89a6..a554194, 1 fix cycle: recess-scenario regression test, review clean; Minor carried: unguarded datetime.strptime on malformed date string)
Task 8: complete (commit a554194..7bcfe4c, review clean, no fix needed); Minor carried: edge case sin cover si valor=None Y sin cache previo (primera corrida), acceso directo con [] en vez de .get() para corrida_exitosa_en (ambos heredados del brief, no del implementador)
Task 9: complete (commit 7bcfe4c..ea95da2, review clean, no fix needed)
Task 6 (cross-fix, post-hoc): fixed window anchoring bug found by Task 10 (commit ea95da2..bb9f971, part of same range) — fetch_cohesion_bloque's dias_ventana window was anchored to datetime.now() regardless of `anio`, making backfill for past years structurally impossible even with network access; fixed to anchor to datetime(anio,12,31) for past years, datetime.now() unchanged for current year; review clean.
Task 10: complete (commits ea95da2..bb9f971, includes cross-fix above; review clean, no further fix needed); Minor carried: backfill never observed producing real data end-to-end (network blocked in this sandbox, correctness rests on mocked unit tests + manual arithmetic verification), cryptic [ERR] list index out of range log line for empty-list derivadas (pre-existing descargar() quirk, out of scope).
Task 11: COMPLETADA CON HALLAZGO CRÍTICO. Se validó vía workflow temporal en GH Actions real (rama feature/itcp-cohesion-bloque-politica, corridas 28890029434/28890253923/28890418081/28890601527, luego eliminado). Hallazgos: (1) el runner de GH Actions NO está bloqueado a nivel IP (a diferencia de este sandbox de desarrollo) — corrida_exitosa_en se setea, POST /votaciones/search responde 200; (2) PERO la respuesta real (26KB, <body class="content-wrapper"> con <div id="loading"> + spinner GIF, 0 ocurrencias de "<tr" y de "redirectActa") confirma que votaciones.hcdn.gob.ar pasó a ser una SPA client-side-rendered en algún momento entre el snapshot de Wayback (verificado en Tarea 4, ene-2026) y hoy (jul-2026) — exactamente el escenario "requiere headless browser" que el research original había descartado. El scraping actual (requests+BeautifulSoup, sin JS) queda estructuralmente inviable contra el sitio actual, más allá de estar bien construido para la estructura que existía en enero. Reportado al usuario para decisión — no se hizo ningún fix automático dado el alcance de la decisión (headless browser nuevo, revertir a manual, u otra fuente).
Task 11 — INVESTIGACIÓN AMPLIADA (post-decisión del usuario de explorar Playwright): se probó Playwright/Chromium headless contra votaciones.hcdn.gob.ar desde GH Actions. Resultado definitivo: el sitio devuelve una página explícita "Acceso temporalmente bloqueado — demasiados intentos fallidos de verificación", IDÉNTICA (mismo texto estático "14 minutos y 58 segundos") en dos intentos separados por 22 minutos — confirma que es un muro de detección de bots permanente/categórico, NO un rate-limit temporal que se resuelve con espaciado. Un headless browser completo NO resuelve el bloqueo (de hecho lo hace más explícito que el shell de carga que veía requests+BeautifulSoup). Se investigó también si Como_voto (terceros) se adaptó: no — su propio dato de Diputados está congelado desde 2026-05-21 (7 semanas), mientras Senado (sitio distinto) sigue fresco hasta 2026-06-23, sugiriendo que el bloqueo es específico del sitio de Diputados y post-fecha esa ventana. cohesion_bloque queda documentado como bloqueado (headless browser incluido) hasta que exista una fuente alternativa o gestión institucional directa con HCDN. Workflows de diagnóstico temporales eliminados de main y de la rama feature.
--- PLAN 2 (ITCP paramétrica) ---
Plan2-Task 1: complete (commit 36b27d2..c965d39, review clean, no fix needed). Implementer found and correctly fixed a real authoring bug in the plan's own test (puntaje_banda(15.0,...) should be 85 not 100, per the established low-exclusive/high-inclusive convention already precedented in test_itcg.py) — verified independently by controller and by reviewer.
Plan2-Task 2: complete (commit c965d39..7e1002b, review clean, no fix needed). Implementer proactively found+fixed the same window-anchoring bug (already fixed for Diputados in bb9f971) baked into this task's own brief code, mirrored the exact fix + a mirrored regression test. LIVE VERIFICATION SUCCEEDED from dev sandbox: senado.gob.ar is reachable and NOT subject to the same anti-bot wall as votaciones.hcdn.gob.ar — real data confirmed (80 actas, 72 votes parsed, valor~99.5-99.7% cohesion). cohesion_bloque_senado is a genuinely viable, working data source.
Plan2-Task 3: complete (commit 7e1002b..16d54b4, review clean, no fix needed). Live-verified against real MAGyP page (16/24 provincias, 66.7%), added a genuine dedup regression test the brief's own tests didn't cover. Minor note: test comment slightly overclaims the malformed-row mechanism vs. what the fixture reproduces (cosmetic only).
Plan2-Task 4: complete (commit 16d54b4..e81f24b, review clean, no fix needed). Real function found via grep: gestion.fetch_protestas_caba() (not the brief's placeholder name) — thin wrapper, no recursion/shadowing risk (verified: gestion.py has zero references to politica, no circular import). NOTE for Task 5: gestion's fetch_protestas_caba() dict includes eventos_2023/var_vs_2023 fields — need to confirm actual scale of "valor" before wiring into itcp.py's BANDAS_ITCP["protestas_caba"] (designed assuming 0-100 like movilizacion_cepa; may need adjustment if valor is a raw count or different scale).
Plan2-Task 5: complete (commit e81f24b..3849688, review clean, no fix needed). Major integration task: calcular_score() removed, itcp.calcular_itcp() wired, cohesion_bloque_senado kept structurally independent from cohesion_bloque (verified). Cross-fix applied+verified: BANDAS_ITCP["protestas_caba"] corrected to %-variation scale (was wrongly 0-100), new _valor_itcp() helper extracts var_vs_2023 specifically for protestas_caba (verified single code path, None-safe). REAL end-to-end run succeeded: exit 1 (expected, cohesion_bloque blocked per ADR-0037), itcp key present (valor=64.7, banda=moderadamente_aflojado, 5 dimensions), cohesion_bloque_senado fetched LIVE successfully in production run. Confirmed politica.fetch_protestas_caba() is a pure passthrough (closes reviewer's one open question about var_vs_2023 preservation).
Plan2-Task 6: complete (commit 3849688..358f31d, verified directly by controller — trivial single-file task, no subagent review needed given near-zero risk).
Plan2-Task 7: complete (commits 358f31d..da94098..11bd7d6, review clean, no fix needed). Self-corrected git incident verified clean in net diff (zero motos content). Both new series functions verified structurally identical to existing patterns; protestas_caba correctly non-duplicated (verified against publicar.py::build_series() merge logic). Disclosed limitation: cohesion_bloque_senado backfill only found 2026 data, not 2023-2025 (real function behavior, not a bug — flagged for future investigation).
Plan2-Task 8: complete (commit 11bd7d6..4d518b7, review clean, no fix needed). All 6 required content items verified present+correct; all quantitative claims in Consecuencias independently recomputed against live output/cache/politica.json and matched exactly. Reviewer flagged a date discrepancy (2026-05-23 vs 2026-04-01) as Important — investigated by controller: NOT an error, 2026-05-23 is the real commit date (584571b) that set the frozen placeholder, 2026-04-01 is the data's own internal fecha_dato field — both legitimately "frozen since" in different senses. No fix needed.
Plan2-Task 9: complete (commit 4d518b7..bfb13f5, review clean, no fix needed). Template-pivot verified real (docs/cinturon_gestion.md IS stale, pre-paramétrica; docs/cinturon_macro.md IS the correct model, correctly followed). Every cited number (ITCP=64.7, all 12 indicator values, bandas table) independently verified byte-exact against output/cache/politica.json. cohesion_bloque's blocked state documented honestly in 4+ places.
Plan2-Task 10: approved with 2 disclosed Important findings requiring follow-up (not a task-10 defect, but blocks true shippability): (1) build-reproducibility risk — web/src/data/informe.json + output/cache/politica.json committed state lacks cohesion_bloque_senado/adhesion_reformas_provincial data, [id].astro has no null-guard, fresh checkout build would crash on the 2 new routes; (2) web/src/lib/descripciones.ts still has the stale "vota alineado con posición oficial" phrase for cohesion_bloque + no entries for 2 new indicators, creating a live public self-contradiction. protestas_caba architectural constraint (flat FICHAS Record, same key reused across cinturones) verified real, dobleUso pattern verified as genuine pre-existing precedent — 2 new + 1 dobleUso-extended is the correct call, not a shortfall.
Plan2-Task 11 (follow-up): DONE_WITH_CONCERNS, commit 2484acf. Finding 1 (build-reproducibility) resolved: real data regenerated (generar_informe.py+publicar.py), verified both new ficha routes build with real data. Finding 2 (descripciones.ts staleness) resolved. NEW gap discovered: pytest tests/test_publicar.py has 2 failures once informe.json is properly regenerated — política's aporte_score reconciliation test still assumes pre-ITCP simple-average, and generar_informe.py's forwarding tuple only has ("itcm","itcg"), missing itcp. Root cause investigated by controller: publicar.py::aplicar_scoring() has dedicated ckey branches for macro/gestion/vida_cotidiana (_scoring_indice, _scoring_vida_itvc) that recompute the paramétrica and set aporte_score per indicator — política has NO such branch, falls through to the old generic SCORING-dict simple-average path. This means the LIVE public site pipeline (publicar.py) never learned about ITCP at all (Task 5 only wired politica.py's own internal main()/cache). Created Task 12 (follow-up) to fix this properly.
Plan2-Task 12 (follow-up): complete (commit 2484acf..da3b85c, review clean, no fix needed). Root cause of test_publicar_genera_snapshot fixed (missing "desactualizado" key in 2 politica.py fetchers). Core wiring verified: publicar.py's new politica/itcp branch, generar_informe.py's forwarding tuple, datos.ts. 3 "beyond-brief" additions all verified as NECESSARY consequences (not scope creep): _anotar_indicadores_itcp() in politica.py (without it aporte_score would be None everywhere), cruz-gating fix in [slug].astro (real UI corruption bug that enabling ITCP would have caused), homepage "3 índices" copy fix. Reviewer independently re-ran tests/test_publicar.py (11/11) and npm run build (64 pages) and inspected generated HTML directly. Full test suite: 93/93. This closes out all known gaps from Plan 2 + its 2 follow-up tasks.
Task 13 (Codex fixes): complete (commit da3b85c..13cc9a9, review clean, no fix needed). All 3 Codex findings verified fixed: (1) P1 legacy-cache guard (_es_cohesion_legado, discriminator "n_actas" not in anterior) — CONFIRMED ACTIVE BUG: output/cache/politica.json's cohesion_bloque currently holds the pre-automation legacy shape (valor=78, estado=placeholder, no n_actas) and was being scored as puntaje_itcp=79.0 (13% weight) — real production corruption, now fixed at the source; (2) desactualizado key added to fetch_cohesion_bloque; (3) new itcp ficha added to fichas.ts, weights verified against itcp.py, no ADR leakage, robustez claim verified correct against publicar.py/sensibilidad.py. Test suite 97/97. One minor note: no end-to-end main()-level regression test for the guard (verified by direct code reading instead) — not blocking.
Final commits (3973d00, 2189df5): both approved. Commit 1 (data purge): verified live cache no longer has cohesion_bloque in any shape (genuinely absent, not corrupted-legacy), test relaxation is mathematically exhaustive (named-set diff catches any of the other 11 going missing, not just a raw count check), ITCP correctly renormalized 64.7->67.4 (hand-verified). Commit 2 (astro build fix): verified the Fragment gate covers ALL unguarded ind.xxx accesses (read full file, not just diff), fallback message syntactically sound. One Important non-blocking note: no safety net distinguishing "genuinely temporary missing data" from "a future ficha id typo" — both would now silently show "Sin dato vigente" instead of crashing; recommended as a follow-up hardening item, not fixed (not an active bug today). BRANCH IS NOW FULLY REVIEWED AND CLEAN: Plan 1 (11 tasks) + Plan 2 (10 tasks + 3 follow-ups) + external Codex review (3 findings) + resulting data-purge/build-fix, all reviewed and approved. Test suite 97/97, web build 65 pages clean.
Task 14 (validación externa ITCP): complete (commit 6d9d1a1..d18d18f, review clean, no fix needed). Final external pair after 2 corrections: EPU Argentina (Banco de España/SECMCA, text-mining based, genuinely independent of riesgo_pais/merval/icc). Real correlation independently recomputed by reviewer from raw data: r=-0.382 (n=30, niveles), r=-0.224 (n=29, diferencias) — correct expected sign, honestly disclosed as moderate given 5/12 thin components. protestas_caba's var_vs_2023 special-case correctly mirrored across itcp.py/politica.py/gestion.py/validacion_externa.py. 4-way matrix extension verified (no hardcoded-3 regression). [slug].astro gating confirmed generic (no política-specific dead code). Tests 97/97, build clean.

--- PLAN 3 (ITCP profundización, 2026-07-08) ---
Profund-Task 1: complete (commit ef52d43..bdd86d4, review clean, no fix needed). _resultado_utilizable() reuses _valor_itcp as single source of truth; reviewer confirmed by substitution it's behaviorally identical to the old gate for all indicators except protestas_caba (the one it targets). 101/101 tests.
Profund-Task 2: complete (commits bdd86d4..24c7699..1927c50, review clean; 1 Minor finding fixed directly by controller — "ver abajo" reference in cinturon_politica.md pointed the wrong direction, corrected to "ver arriba, ADR-0037"). Critical risk (motos WIP leakage in descargar_series.py) verified clean by reviewer via hunk-count + grep.
Profund-Task 3: complete (commit 1927c50..b439805, review clean, no fix needed). ITCP entry mirrors itcm/itcg shape exactly; dominant leave-one-out component confirmed cohesion_bloque_senado (Δ -8.1), consistent with published robustez.dominante. 101/101 tests.
Profund-Task 4: complete (commit b439805..a4719a0, review clean, no fix needed; 1 mid-task resume needed — implementer sent a confused "waiting for background notification" message with work already done through Step 6, resumed via SendMessage to finish Steps 7-10). LIVE-VERIFIED against real senado.gob.ar: cohesion_bloque_senado now 4 annual points (2023:81.8, 2024:94.5, 2025:91.4, 2026:99.7), up from 1. _paced_post confirmed byte-for-byte mirror of _paced_get by reviewer (checked live source, not just diff). Incidental ratio_dnu drift (1.471->1.529) in the same politica.csv regen, disclosed, same class as prior sessions' documented live-refetch drift.
Profund-Task 5: complete (commits a4719a0..fdf8c69..b2320f8..abe7b0c). Real mid-task discovery: brief's curated fixtures for CEPA report 748 didn't match the real full page, which contains a second sentence hitting the pre-existing m_mes regex branch -- would have produced a metric-inconsistent 3-point series (mixing monthly-rate and cumulative-count on different scales). Controller diagnosed root cause (via advisor) and directed a metric-consistency filter (rama field m_tot/m_mes) rather than debugging which number to extract. 1st review: Approved with 2 Important findings (stale docstring, fragile string-suffix filter) -- fixed in b2320f8, re-reviewed clean. Found+fixed 1 more residual doc contradiction myself post-re-review (abe7b0c, doc-only, no subagent needed). LIVE-VERIFIED final state: movilizacion_cepa = exactly 2 points (773->46.0 2026-04-09, 809->50.5 2026-06-09), matching live cache's own headline value. 110/110 tests. Also: implementer self-caught and corrected a git-staging mistake (git commit -- <pathspec> pulled in unrelated motos WIP; caught via git show --stat, reset --soft + reset -p, clean recommit) -- verified independently by controller.
Profund-Task 6: complete (commits abe7b0c..31854ac..9fe4a0a). Real values verified independently by controller against live output/cache/politica.json before dispatching reviewer (score=3.3, ITCP=67.2, cohesion_bloque_senado=99.4/4-june, movilizacion_cepa=50.5/9-june) -- matched exactly. Commit scoped to exactly 1 file both times. Review: Approved with 1 Important finding (missing "cumulative-since-January" caveat on movilizacion_cepa's 2-point backfill, flagged by Task 5's own report as a documentation debt) -- fixed directly by controller (9fe4a0a), no subagent cycle needed for a 1-line doc caveat. Implementer also proactively fixed 2 residual "degrada a cache" stale-language inconsistencies + a CEPA report date, verified genuine and in-scope by reviewer.
--- PLAN 3 COMPLETE: all 6 tasks done, each reviewed (5/6 first-pass approved w/ minor-or-no fixes, 1/6 needed a real mid-task design correction via advisor consult). Recurring operational pattern this session: implementer subagents on long live-network tasks (4,5,6) repeatedly stalled with a false "waiting for background notification" message after doing real work -- resumed successfully each time via SendMessage, no work lost. Next: final whole-branch review.
Final whole-branch review (base d18d18f, head 9fe4a0a, 12 commits, opus model): Ready to merge = Yes. No Critical/Important. 4 Minor findings: (1) real cross-task finding -- fetch_cepa_movilizacion() (live indicator) lacked the rama=="m_tot" guard that fetch_cepa_movilizacion_serie() (backfill) got in its fix cycle; latent today (newest CEPA report always m_tot) but a real future risk (an "aniversario"-type report as newest would score wrong-scale data silently); (2) rama key leaking into published dict (same root cause); (3) stale "GET" verb in _descubrir_actas_senado docstring; (4) sensibilidad.json drift note (confirmed pre-existing/unrelated, not a defect). User chose to fix (1)/(2)/(3) now rather than leave as debt: fixed docstring verb (5f98b4c) and added the same rama guard + regression test to fetch_cepa_movilizacion() (8157f28, 111/111 tests). PLAN 3 FULLY COMPLETE: 6 tasks + 1 whole-branch review + 4 post-review fix commits, all on feature/itcp-validacion-externa. Next: finishing-a-development-branch.

--- PLAN 4 (conflictividad_laboral_srt reemplaza movilizacion_cepa, 2026-07-08) ---
Plan4-Task 1: complete but plan ABANDONED after Task 1 (commit 14cd9fc..5776f26). Task 1's live investigation found the plan's core premise was wrong: SRT (Secretaría de Trabajo) discontinued quarterly conflictividad-laboral reports after 2020t2 and has NO data of any kind past March-2023 (confirmed authoritatively via the canonical Google Spreadsheet feeding the official listing page, cross-validated against 32/32 HEAD-verified URLs) -- cannot serve as a live replacement for movilizacion_cepa. Proposed fallback (SRT's monthly xlsx 2006-2023 as a new validación externa pair) also found infeasible on further analysis: zero temporal overlap between SRT's series (ends 2023-03) and the ITCP's own reconstructed monthly series (starts 2023-12, dic-2023 Milei baseline) -- no correlation is computable. User decision: close this front, no further code changes, keep movilizacion_cepa as already fixed earlier today (rama/m_tot guard). Tasks 2-6 of docs/superpowers/plans/2026-07-08-conflictividad-laboral-fuente-oficial.md are NOT executed and will not be -- plan superseded by this finding. The manifest (data/politica/srt_conflictividad_urls.json) and discovered xlsx URL remain committed as documented research, no live use today.

--- PLAN 5 (alineamiento_senadores_prov reemplaza gobernadores_alineamiento, 2026-07-08) ---
Plan5-Task 1: complete (commit bb6b75c..557500f, review clean, no fix needed). _parsear_acta extracts celdas[3] as "provincia", additive, verified byte-exact against real FIXTURE_ACTA (Córdoba/Entre Ríos/CABA). Both downstream consumers (fetch_cohesion_bloque/_senado) confirmed unaffected (key-lookup access only). 119/119 tests.
Plan5-Task 2: complete (commits 557500f..9e6a180..e25dd17). Review: Approved with 1 Important finding (fecha_max gated on global accumulator state instead of per-acta contribution, defect originated in the plan's own brief code, not implementer deviation) + 1 coverage gap (multi-acta accumulation never exercised by original 3 tests). Both fixed directly by controller: 1-line fix mirroring fetch_cohesion_bloque_senado's existing per-acta gating pattern, plus 1 new regression test covering both the fix and the multi-acta merge math (hand-verified arithmetic: CABA (1,2)=50%, Córdoba (1,3)=33.3%, avg=41.7%, fecha_dato correctly stays at last-contributing acta not latest-overall). LIVE-VERIFIED by implementer against real senado.gob.ar: valor=68.3, n_provincias=24 (max possible), fecha_dato=2026-06-04 (confirmed genuine inter-session gap, not a bug). Disclosed characteristic (not a defect): metric includes consensual/unanimous votes, not just contested ones -- confirmed consistent with pre-existing indice_rice/cohesion_bloque_senado precedent in this codebase, not a new inconsistency. 123/123 tests.
Plan5-Task 3: complete (commits 96ed997 [reverted] -> 4c15dfb, clean recommit). Review: 1 Critical finding -- implementer's `git add scripts/descargar_series.py` (as literally instructed by the brief) staged the whole file, sweeping in 3 unrelated motos-WIP hunks (undisclosed in implementer's own report) alongside the 3 real alineamiento_senadores_prov hunks. Controller fixed directly: git reset HEAD~1 (unstage everything), git add -p isolating exactly the 3 correct hunks (verified via git diff --cached grep), re-added test file + real backfill artifacts (politica.csv, alineamiento_senadores_serie.json), recommitted clean. 124/124 tests. LIVE-VERIFIED backfill preserved through the split: 2023=71.9%, 2024=57.7%, 2025=45.3%, 2026=51.9%. Lesson for future briefs in this plan: "git add <file>" instructions on descargar_series.py are unsafe while the motos WIP remains uncommitted in the working tree -- should say "git add -p" explicitly, not just "git add".
Plan5-Task 4: complete (commit 4c15dfb..89e7ed0, review clean). Self-reported deviation verified sound by reviewer: brief's literal finite top band (65,100,100) would score real value 68.3 at ~92 not 100 under the interpolation engine's midpoint-anchoring (parametrica.py), fixed to open ±INF tail matching file-wide convention -- independently reproduced the ~99.5 failing-test value via manual recomputation. Live: alineamiento_senadores_prov=68.3, puntaje=100.0, ITCP=68.4 (up from 67.2), frescos=11/12. gobernadores_alineamiento correctly preserved (not deleted) as inert historical band entry. Controller then fixed 1 cross-task finding directly (commit 5613346): test_publicar.py hardcoded old indicator name, now expects new name -- correctly red until Task 5 regenerates informe.json (not a regression). Controller also discovered + scoped a NEW Task 6 (web layer sync: fichas.ts/descripciones.ts/datos.ts still reference gobernadores_alineamiento by name; label() falls back to raw key, same bug class as Plan2-Task10/11) and appended it to the plan file (commit 38b1e2c) with complete real code, not yet executed.
Plan5-Task 5: complete (commits 89e7ed0..5613346..a84aad7..a302ac9, review clean, no fix needed). Doc sync verified internally consistent by reviewer (ITCP 68.4/tensión 3.2 arithmetic checks out, 67.2->68.4 delta plausibly reconciled against band-swap + ratio_dnu drift). gobernadores_alineamiento retirement narrative preserved (4 discarded proxies restated, not scrubbed), honest caveat present. Controller found+fixed 1 more real issue directly (a302ac9): gate_calidad.py G3 failing for alineamiento_senadores_prov (card 90d window vs backfill 366d window) -- same precedented pattern as 3 existing G3_EXCEPCIONES entries, verified real via code read (not a weakened check). Remaining 3 G3 failures (macro/iai, vida_cotidiana x2) confirmed pre-existing drift, out of scope, untouched. 125/125 tests. Next: Task 6 (web layer sync, already written into the plan).
Plan5-Task 6: complete (commit a302ac9..96d9b8e, review clean, no fix needed). Implementer correctly flagged a genuine self-contradiction in the brief itself (Step 1 code keeps retired datos.ts entries, Step 5's expectation said it shouldn't) rather than silently resolving it. Reviewer independently traced every consumer of LABELS/UNIDADES_*/BARRA_0_100 across web/src (none iterate these maps, all are keyed lookups) and confirmed keeping the retired entries is not just harmless but REQUIRED (label(f.id) is called unconditionally for the retired ficha's own page title/heading -- without the kept entry it would show the raw unformatted key). Build verified clean with concrete evidence (66 pages, /metodologia/alineamiento_senadores_prov/index.html generated). All cross-references updated in both locations. --- PLAN 5 COMPLETE: all 6 tasks done, each reviewed, 4 real bugs caught+fixed across the plan (motos-WIP leak x2 in Task 3, fecha_max gating bug in Task 2, band-interpolation midpoint bug in Task 4, gate_calidad G3 gap + cross-task test staleness after Task 4). Next: final whole-branch review across all 6 tasks (bb6b75c..96d9b8e).
Final whole-plan review (Plan 5, base bb6b75c, head 96d9b8e, opus model): Ready to merge = Yes with follow-ups. No Critical findings. Cross-task integration verified consistent end-to-end (scraper->parser->aggregator->fetch->itcp.py->publish->web). Caveat consistency verified present on all 4 surfaces (code docstring, web ficha, web descripcion, project doc). 1 Important finding: validacion_externa.py::ITCP_SERIES still hardcoded the retired indicator name and omitted the new one -- non-crashing (calcular_itcp silently renormalizes) but numerically real: verified impact r=-0.371 (stale) -> r=-0.362 (corrected) on the published ITCP<->EPU validation, same sign/conclusion. Fixed directly by controller (d7d638f): swapped the name, refreshed docstring, re-ran validacion_externa.py, verified live output matches reviewer's independent recompute exactly. 2 Minor findings (stale sensibilidad.json + series.json leave-one-out/historical stub) also fixed by controller: traced root cause to data/historico/indicadores.json (the "red de seguridad" accumulator) still holding 2 residual points (55.0 x2) that fusionar_historico() re-injects every publicar.py run -- same bug class as the cohesion_bloque legacy-cache purge earlier today (3973d00). Purged (610fa7d); confirmed acumular_historico() cannot reintroduce it (only upserts indicators present in informe.json, and gobernadores_alineamiento is gone from there since Task 4). Full pipeline re-run clean: 125/125 tests, gate_calidad shows only the same 3 pre-existing out-of-scope macro/vida_cotidiana drift failures (unchanged before/after, confirmed not newly introduced). PLAN 5 FULLY COMPLETE: 6 tasks + 1 whole-branch review + 2 post-review fix commits (d7d638f, 610fa7d), all directly on main. Ready to push.
POST-REVIEW INCIDENT + FIX: controller's own purge commit (610fa7d) broke the GitHub Pages deploy -- confirmed via `gh run list`: CI failed with "Ficha gobernadores_alineamiento: no hay dato vigente NI serie histórica" ([id].astro's defensive guard, from earlier session's 2189df5 fix). Root cause: the 2 "residual" points in data/historico/indicadores.json (55.0 x2) were NOT dead weight -- they were the only thing satisfying the guard for the retired ficha's still-generated route (fichas.ts intentionally keeps it, getStaticPaths() iterates all keys). Reverted immediately (ccb9d33): restored the 2 points, republished, verified LOCAL build clean (66 pages) before pushing again. Re-verified live in production: both /informe/politica/ and /informe/metodologia/alineamiento_senadores_prov/ and /informe/metodologia/gobernadores_alineamiento/ return 200; live ficha page shows real value 68,3%. Deploy run succeeded (confirmed via gh run list). PLAN 5 FULLY COMPLETE AND LIVE IN PRODUCTION: 6 tasks + final review + 3 post-review fix commits (d7d638f validacion_externa fix, 610fa7d historico purge [later partially reverted], ccb9d33 revert-of-purge fixing the Pages break). Lesson for future purges: before purging any indicator's leftover data, check whether its ficha/route still exists and needs SOME data source (live or serie) to avoid a build-time crash -- purging is not always strictly safe even when a value looks like "residue," if a page-generation guard depends on its mere presence.

--- PLAN: org-cigob-migracion (2026-07-12) ---
Task 1: complete (commit a700907..6659838, review clean; Important note carried to Task 2: pages.yml cp step + actualizar_encuestas.py still reference deleted projects/votometro/ path, both fixed by Task 2, not yet pushed to main so no live breakage).
Task 2: complete (commits 6659838..f0114aa..0380339, 1 fix cycle: implementer scope-crept into removing bibliotecario.html row from README (Task 3 turf), reverted directly by controller in 0380339, re-verified grep clean). Important note: pages.yml cp step + actualizar_encuestas.py dangling-path risk from Task 1 fully resolved here.
Task 3: complete (commit 0380339..e9639fd, review clean, no fix needed). Minor carried: dead .tag-ia/.tag-electoral CSS rules in web/index.html now unused (out of scope, not fixed).
Task 4: complete (verification + push, commits e9639fd pushed to main, no new commit needed for this task itself). Ran directly by controller (not delegated) given production-push risk. 264/264 tests passing. Found + discarded 2 classes of working-tree side effects before push: (1) real pytest test-isolation bug — running `pytest tests -q` alone mutates data/historico/indicadores.json, web/src/data/informe.json, web/src/data/series.json with stale content (reproduced twice, confirmed causal) -- NOT fixed, out of scope, flagged to user as a separate issue; (2) politica.py smoke-test run legitimately updated output/cache/politica.json + data/politica/derrotas_legislativas_eventos.json with live data -- discarded since this task only needed a crash-free smoke test, not a full scoped publish (would need generar_informe.py+publicar.py+gate_calidad.py per repo convention to publish properly). Also fixed 2 more dangling votometro references found outside Task 2/3 file lists: CLAUDE.md and AGENTS.md (both gitignored, not part of any commit, updated directly for future-session accuracy). Verified web/vida_cotidiana/collectors/manual.py:112 votometro.html mention is inert doc-string metadata in a not-yet-automated manual-indicator catalog (no functional coupling, correctly left untouched, same class as docs/cinturon_politica.md/archivo). Post-push: gh run watch confirms pages.yml deploy succeeded end-to-end (Astro build, votometro copy step correctly absent, publish to cigob-informe gh-pages), informe.cigob.org cert still approved. PHASE 1 (Tasks 1-4, all local/automatable cleanup) COMPLETE AND LIVE. Next: Task 5 is a manual blocker (org creation, no API) -- stopped here per plan, waiting on user.
Task 6 (consolidacion, deviating from original plan per user mid-course decision): main repo transferred+renamed to Fundacion-CIGOB/cigob-informe-coyuntura (succeeded). Deploy repo transfer to Fundacion-CIGOB/informe-deploy caused REAL INCIDENT: custom domain cname reset to null on transfer (expected per plan), but private-Pages required paid ORG plan (Fundacion-CIGOB is Free) -- 422 blocked. Reverted informe-deploy back to Juanpintoselso33/cigob-informe; made public to work around Free-plan private-Pages limit; while fixing, an errant PUT cname=(empty) 404d and fully disabled the Pages site object (not just cname); recreated via POST build_type=workflow; repeated cname PUT attempts all failed "already taken" despite zero verified domains on account -- root cause found via user checking GitHub web UI directly: GitHub now requires fresh TXT-record domain verification (_github-pages-challenge-Juanpintoselso33.informe.cigob.org, value f6bfab62999955ba5ad4aeefe8d333) before allowing custom-domain assignment, unrelated to plan tier. DNS is Wix-managed (Santiago Simons), propagation up to 24h -- NOT YET RESOLVED, live site currently coasting on stale CDN cache (consistently HTTP 200 across ~15 checks over ~40min, underlying mechanism unclear). User then pivoted mid-incident: requested consolidating to ONE repo total (not two) instead of restoring the two-repo topology, explicitly overriding controller-recommended "stabilize domain first" ordering. Executed: astro.config.mjs simplified to single build (site=informe.cigob.org, base "/", outDir web-dominio, dropped DEPLOY_TARGET branching + the stale hardcoded old-reponame base path bug this exposed), pages.yml dropped the dual-build + peaceiris external-repo-push mechanism entirely (single build -> upload-pages-artifact path=web-dominio -> deploy-pages direct from this repo), web/index.html (dead multi-tool landing page, no longer part of any published artifact) deleted, web/README.md + root README.md + .gitignore updated to match (commit be8fa5e, pushed, local git remote updated to new URL). Local build verified working (64 pages) but the actual deploy-pages step in CI fails 422 "Validation Failed" -- SAME plan restriction (private repo + Free org) now blocking the consolidated repo too, confirming user will need Team-tier org billing (not Enterprise -- corrected a user misconception; Enterprise is for a different private-Pages-VISIBILITY feature, not this) regardless of consolidation. Juanpintoselso33/cigob-informe archived (not deleted) as the now-fully-deprecated old deploy repo. CIGOB_INFORME_DEPLOY_KEY secret deleted from the consolidated repo (no longer used). CURRENT BLOCKING STATE, needs user action on 2 independent fronts before Pages is fully healthy again: (1) DNS TXT record via Santiago Simons for domain re-verification, (2) org billing upgrade to GitHub Team for private-repo Pages. Neither is done yet. Site itself still returning 200 live as of last check.

**Plan próxima semana (2026-07-12):** Juan va a hacer una demo en vivo al equipo mostrando el flujo E2E (conectar Claude/ChatGPT al repo) ANTES de agregarlos como colaboradores reales — recién ahí ellos se crean cuenta GitHub y se los suma. No pedir de nuevo usernames hasta que Juan confirme que la demo ya pasó. La demo no depende del incidente de informe.cigob.org (acceso a repo privado + conector IA es independiente de Pages/dominio).
